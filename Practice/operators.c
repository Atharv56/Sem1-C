#include <stdio.h>

int main(){
    int a = 12, b = 3;
    printf("Addition - %d\nSubtraction - %d\nMultiplication - %d\nDivision - %d\nModulus - %d\nincrement - %d\ndecrement - %d", 
    a+b, a-b, a*b, a / b, a % b, ++a , --b 
    
    );
    return 0;
}